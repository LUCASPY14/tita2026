[pytest]
DJANGO_SETTINGS_MODULE = backend.settings.test
python_files = tests/*.py tests/**/*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers --cov-fail-under=80 -m "not stress"
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    integration: marks integration tests that require DB
    stress: marks stress/concurrency tests — excluidos del CI normal (correr con -m stress)
